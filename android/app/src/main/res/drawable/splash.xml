<?xml version="1.0" encoding="utf-8"?>
<!--
  Shown by the Capacitor SplashScreen plugin while the WebView boots. The
  background resolves per theme (values/ vs values-night/), so the handover to
  the rendered app has no colour flash in either mode.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splashBackground" />
    <item
        android:width="96dp"
        android:height="96dp"
        android:gravity="center"
        android:drawable="@drawable/ic_splash_logo" />
</layer-list>
